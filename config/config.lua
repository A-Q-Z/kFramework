
Config = {}

-- Database configuration
Config.Database = {
    host = 'localhost',
    user = 'root',
    password = 'password',
    database = 'fivem_db'
}

-- Server settings
Config.Settings = {
    maxPlayers = 32,
    language = 'en',
    debugMode = false
}

-- Modules activation
Config.Modules = {
    enableJobs = true,
    enableEconomy = true,
    enableVehicles = true
}
